"use strict";

var FindPeakElementInArray = function(array){
	this.array = array;
	return evaluate();
}

var evaluate = function(){
	var array = this.array;

	if(!array.length) return null;

	if(array.length === 1) return array[0];

	

	
}

var iterate = function(arr, start, end){
	var m = Math.floor(arr.length/2);

	if(m === start && arr[m + start] > )
}